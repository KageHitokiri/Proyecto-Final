<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <meta name="description" content="" />
        <meta name="author" content="" />
        
        <!-- Favicon-->
        <link rel="icon" type="image/x-icon" href="assets/favicon.ico" />
        <!-- Bootstrap icons-->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css" rel="stylesheet" />
        <!-- Core theme CSS (includes Bootstrap)-->
        
        <script src="js/jquery-3.6.0.js"></script>
        <script src="js/jquery-ui-1.13.1.custom/jquery-ui.js"></script>            
        <script src="js/effects.js"></script>    

        <title>{% block title %} {% endblock %}</title>        
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}            
            <link href="css/styles.css" rel="stylesheet" />
            <link href="css/general.css" rel="stylesheet" />
        {% endblock %}

        {% block javascripts %}        

        {% endblock %}
    </head>

    <body>

            <!-- Responsive navbar-->
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container px-lg-5">
                <a class="navbar-brand" href="/">DicePals</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                        <li class="nav-item"><a class="nav-link {{ app.request.get('_route') == 'index' ? 'active' }}" aria-current="page" href="/">Página principal</a></li>
                        <li class="nav-item"><a class="nav-link {{ app.request.get('_route') == 'wiki' ? 'active' }}" href="wiki">Wiki</a></li>
                        {% if app.user %}
                            <li class="nav-item"><a class="nav-link {{ app.request.get('_route') == 'play' ? 'active' }}" href="play">Juega ahora</a></li>                        
                            <li class="navitem"><a id='userInfoDiv' userId='{{app.user.id}}' userName='{{app.user.email}}' class="nav-link {{ app.request.get('_route') == 'logout' ? 'active' }}" href="logout">{{app.user.email}} - Cerrar Sesión</a></li>
                        {% else %}
                            <li class="nav-item"><a class="nav-link {{ app.request.get('_route') == 'register' ? 'active' }}" href="register">Registrarse</a></li>
                            <li class="nav-item"><a class="nav-link {{ app.request.get('_route') == 'login' ? 'active' }}" href="login">Iniciar Sesión</a></li>                            
                        {% endif %}
                        <li class="nav-item"><a class="nav-link {{ app.request.get('_route') == 'about' ? 'active' }}" href="about">Acerca de...</a></li>
                    </ul>
                </div>
            </div>
        </nav>




        


        {% block body %}        

        {% endblock %}

                <!-- Footer-->
        <footer class="py-5 bg-dark">
            <div class="container"><p class="m-0 text-center text-white">Copyright &copy; CarlosRamosDev 2021 - 2022</p></div>
        </footer>


        <!-- Bootstrap core JS-->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
        <!-- Core theme JS-->
        <script src="js/scripts.js"></script>

    </body>
</html>
